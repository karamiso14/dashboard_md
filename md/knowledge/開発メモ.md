# 開発メモ

## 環境整備メモ
- winのvscodeをwslに接続、wslの中でdockerで起動している
- winにgoを入れなくてもwsl側でカバーしてる（これじゃないとフォーマッタとか効かないしgoまわりが面倒)
- wslの領域でcloneしてるので速度低下もしない

## 進捗メモ

とりあえずカードUIと検索が動くようになった
一通り形はできたので、折を見て作業PC側に持っていく準備をする

- main.goが読みにくいので、codexに機能そのままにリファクタリングさせる
- あとは構造がようわからんのでそのへんも確認する

全文検索にさせた
規模デカくなってきたら起動時とか更新時にインデックスキャッシュする仕組みを入れるといいそう